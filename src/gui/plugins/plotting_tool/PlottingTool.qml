import QtQuick 2.0
import QtCharts 2.3
import QtQuick.Controls 1.4
import QtQuick.Controls.Styles 1.4
import QtQuick.Layouts 1.3

/*
This qml may have to be moved to ign-gui.

Parts that list TopicsModel => ign-gui
Parts that go into PlottingInterfacte => ign-gui
    => used by ign-gazebo by install plugin header? 
*/
